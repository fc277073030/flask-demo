apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-demo-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flask-demo-chart.name" . }}
    helm.sh/chart: {{ include "flask-demo-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flask-demo-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
